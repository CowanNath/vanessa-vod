services:
  vanessa-vod:
    build: .
    container_name: vanessa-vod
    ports:
      - "8608:8608"
    volumes:
      - downloads:/app/downloads
      - app-data:/app/data
    restart: unless-stopped

volumes:
  downloads:
  app-data:
